language: node_js
node_js":
  - 16.15

services:
  - docker

script:
  - docker login -u ${DOCKER_USERNAME} -p ${DOCKER_PASSWORD}
  - docker images
  - docker tag udagram-api-feed edwardmuss/udagram-api-feed:latest
  - docker tag udagram-api-user edwardmuss/udagram-api-user:latest
  - docker tag udagram-frontend edwardmuss/udagram-frontend:latest
  - docker push edwardmuss/udagram-api-feed:latest
  - docker push edwardmuss/udagram-api-user:latest
  - docker push edwardmuss/udagram-frontend:latest

# deploy:
#   provider: script
#   script: bash docker_push
#   on:
#     branch: master